"use client";

export const walletIntegrationStatus = {
  enabled: false,
  reason: "Reown/Wagmi wallet integration is intentionally disabled in the Render-safe starter build. Add NEXT_PUBLIC_REOWN_PROJECT_ID and wallet dependencies when enabling non-custodial trading."
};
