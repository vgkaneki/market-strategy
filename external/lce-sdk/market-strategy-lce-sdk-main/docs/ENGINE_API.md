# Engine API

Use `createLceEngine` from `packages/lce-core/src/index.mjs`.

Methods:

```txt
ingestCandles(symbol, timeframe, candles)
ingestOrderBook(symbol, book)
ingestTrades(symbol, trades)
getLevels(symbol)
getZones(symbol)
getStatus()
```
